# LangChain Agent 入门：从配置到 DeepSeek 接入

## 1. 本次课程目标

完成一个可以调用工具的 LangChain Agent：用户询问天气时，模型识别需求并调用本地定义的天气工具，再把结果组织为自然语言回答。

## 2. 项目结构与常用命令

```text
Agent-Demo/
├── .env          # API 密钥和服务配置（不可提交）
├── src/index.ts  # Agent 入口
├── package.json  # 依赖和脚本
└── tsconfig.json # TypeScript 配置
```

```bash
pnpm dev    # 用 tsx 直接运行 TypeScript 入口
pnpm build  # 使用 tsc 做类型检查与编译
```

`package.json` 中的 `dev` 脚本必须和实际文件位置一致。本项目入口是 `src/index.ts`，因此使用：

```json
"dev": "tsx src/index.ts"
```

## 3. Agent 的核心组成

1. **环境变量**：通过 `import "dotenv/config"` 读取 `.env` 中的配置。
2. **模型**：`ChatOpenAI` 是 OpenAI 兼容接口的模型客户端；也可以通过更换地址和模型名接入 DeepSeek。
3. **工具**：用 `tool()` 定义 Agent 可调用的函数，并用 Zod 描述输入参数。
4. **Agent**：`createAgent({ model, tools })` 把模型和工具组合起来。
5. **调用方式**：
   - `agent.invoke()`：等待完整结果后一次性返回。
   - `agent.stream()`：逐步接收输出，适合实现终端“打字机”效果。

本项目的天气工具示例：

```ts
const getWeather = tool(
  ({ city }) => `${city} 今日天气：晴，气温 22°C，湿度 60%`,
  {
    name: "get_weather",
    description: "查询指定城市的当前天气",
    schema: z.object({ city: z.string() }),
  },
);
```

## 4. 配置 DeepSeek

DeepSeek 提供 OpenAI 兼容 API，因此无需替换 LangChain 客户端，只需要调整配置。

`.env`：

```env
DEEPSEEK_API_KEY=你的 DeepSeek API Key
DEEPSEEK_API_URL=https://api.deepseek.com
```

`src/index.ts`：

```ts
const model = new ChatOpenAI("deepseek-v4-flash", {
  apiKey: process.env.DEEPSEEK_API_KEY,
  configuration: {
    baseURL: process.env.DEEPSEEK_API_URL,
  },
});
```

注意：密钥只应存放在 `.env`，不要写进源码、截图、聊天记录或 Git 仓库。

## 5. 本次遇到的报错与排查

| 报错 | 原因 | 解决方法 |
| --- | --- | --- |
| `ERR_MODULE_NOT_FOUND: src/index.ts` | `dev` 脚本指定的入口文件不存在 | 创建对应文件，或把脚本改为实际入口路径 |
| `404 Invalid URL (…/chat/completions/chat/completions)` | `baseURL` 已包含接口路径，SDK 又自动追加了一次 | `baseURL` 只保留 API 根地址，例如 `https://api.openai.com/v1` |
| `404 The model openai:gpt-4o does not exist` | 把 LangChain 的模型快捷写法误用于 `ChatOpenAI` 构造函数 | 直接传 API 模型名，例如 `gpt-4o` |
| `429 insufficient_quota` | OpenAI API Key 没有可用额度 | 在 API Platform 单独开通 API 计费或充值 |
| `TS2591: Cannot find name process` | TypeScript 未加载 Node.js 类型 | 在 `tsconfig.json` 加入 `"types": ["node"]` |
| `LangSmith 403` | 可选的 LangSmith 追踪服务鉴权失败 | 更新 LangSmith Key，或将 `LANGSMITH_TRACING=false` |

## 6. 重要概念：Codex/ChatGPT 订阅与 API 账单

Codex 或 ChatGPT 的会员权益，不会自动提供项目中 `OPENAI_API_KEY` 的 API 调用额度：

- 在 Codex 应用内使用模型，消耗的是 Codex/ChatGPT 的套餐额度。
- 在代码中用 API Key 请求 `api.openai.com`，消耗的是 API Platform 的独立额度。
- 因而 API 的 `429 insufficient_quota` 需要通过 API Platform 的计费设置处理，不能用 Codex 会员直接解决。

## 7. 推荐排错顺序

1. 先读完整错误信息，特别是 HTTP 状态码、实际请求路径和模型名。
2. 确认入口文件和 `package.json` 脚本匹配。
3. 确认 `.env` 中变量名与代码中的 `process.env` 一致。
4. 确认 `baseURL` 是服务根地址，不重复包含具体接口路径。
5. 确认模型名是该服务商真实支持的模型名。
6. 先执行 `pnpm build`，再执行 `pnpm dev` 验证完整链路。
7. 遇到 401/403 检查密钥和权限；遇到 429 检查额度、限流和账单。

## 8. 下一步练习

- 把静态天气工具替换为真实天气 API。
- 为工具增加更多参数，例如日期、温度单位和天气预警。
- 给 `agent.stream()` 加上消息类型判断，只输出最终文本内容。
- 处理 API 调用异常，向用户展示明确的错误提示。

---

# 第三章：模型抽象层与消息的输入输出

> 课程原文：[深入浅出 LangChain —— 第三章：模型抽象层与消息的输入输出](https://juejin.cn/post/7634002571020271635)

## 1. 本章核心目标

理解 LangChain 如何用统一接口调用不同大模型，并掌握消息格式、流式输出和结构化输出三项基础能力。

一句话总结：**把模型视为可替换的底层能力，业务逻辑不应绑定某一家模型提供商。**

## 2. 为什么需要模型抽象层

不同 Provider 的原生 SDK 在认证、参数、返回结果、流式协议和错误格式上均有差异。若业务代码直接调用原生 SDK，切换模型时通常需要重写大量代码。

LangChain 为 OpenAI、Anthropic、Google、Ollama 等提供统一的模型调用接口，使应用可以较低成本地切换模型，并统一处理消息、流式响应和异常。

## 3. 两种模型配置方式

### 字符串标识符：适合快速验证

```ts
import { createAgent } from "langchain";

const agent = createAgent({
  model: "openai:gpt-4o-mini",
});
```

- 写法简洁，适合练习、原型和简单配置。
- LangChain 会解析 `provider:model-name`，并在内部创建对应的 ChatModel。
- 并非所有第三方或国产模型都能被该快捷写法自动识别。

### 显式实例化：适合正式项目

```ts
import { ChatOpenAI } from "@langchain/openai";

const model = new ChatOpenAI({
  model: "gpt-4o-mini",
  temperature: 0.1,
  maxTokens: 2_000,
  timeout: 30_000,
  maxRetries: 3,
});
```

- 可以精确控制温度、输出长度、超时和重试策略。
- 适合自定义 API 地址、OpenAI 兼容接口和长期维护的业务代码。
- 企业项目通常优先采用这种方式，配置更明确、可扩展性更好。

### 常见参数

| 参数 | 作用 | 建议 |
| --- | --- | --- |
| `temperature` | 控制输出随机性 | 工具调用、提取任务用 `0 ~ 0.2`；创意内容可提高 |
| `maxTokens` | 限制最大输出长度 | 按任务设置，避免成本和输出失控 |
| `timeout` | 单次请求超时 | 常用 `30 ~ 60` 秒 |
| `maxRetries` | 临时失败重试次数 | 常用 `2 ~ 3` 次 |
| `topP` | 控制采样范围 | 通常不必与 `temperature` 同时大幅调整 |

注意：不同模型的参数要求不同；例如 Anthropic 模型常需要显式指定 `maxTokens`。模型名、可用性和价格应以 Provider 最新文档为准。

## 4. Provider 接入思路

- **OpenAI**：使用 `@langchain/openai` 的 `ChatOpenAI`。
- **Anthropic**：使用 `@langchain/anthropic` 的 `ChatAnthropic`，适合长上下文与复杂推理场景。
- **Google Gemini**：使用 `@langchain/google-genai` 的 `ChatGoogleGenerativeAI`，具备多模态和较高免费额度等特点。
- **Ollama**：使用 `@langchain/ollama` 的 `ChatOllama`，适合数据不出本机或离线场景。
- **OpenAI 兼容服务**：通常仍可用 `ChatOpenAI`，传入该服务的 `apiKey` 与 `configuration.baseURL`。

敏感数据优先考虑本地部署；开发调试优先使用低成本小模型；复杂推理和长文档任务再按效果选择更强模型。

## 5. 消息格式与多轮对话

```ts
const messages = [
  { role: "system", content: "你是一个专业的 TypeScript 助手。" },
  { role: "user", content: "什么是泛型？" },
  { role: "assistant", content: "泛型允许类型作为参数传入。" },
  { role: "user", content: "请给一个例子。" },
];
```

| 角色 | 用途 |
| --- | --- |
| `system` | 设置角色、规则、背景与边界 |
| `user` / `human` | 用户的当前输入 |
| `assistant` | 模型先前回复，用于保留上下文 |
| `tool` | 工具调用结果，供 Agent 继续推理 |

多轮对话的“记忆”本质上是：每次请求都将历史消息连同当前问题一起发送给模型。模型本身并不会自动保存对话历史。

简单场景可直接使用 `{ role, content }` 对象；需要额外元数据或复杂操作时，再使用 `SystemMessage`、`HumanMessage`、`AIMessage` 等消息类。

## 6. 一次性调用与流式输出

```ts
const result = await model.invoke(messages);
console.log(result.content);
```

`invoke()` 会等待模型生成完毕后一次性返回，适合后台任务、短输出或只关注最终结果的场景。

```ts
const stream = await model.stream(messages);

for await (const chunk of stream) {
  process.stdout.write(chunk.content as string);
}
```

`stream()` 返回异步迭代器，适合聊天界面和长文本生成，可以让用户立即看到逐步输出的内容。

Agent 同样支持流式调用：

```ts
const stream = await agent.stream(
  { messages: [{ role: "user", content: "解释 TypeScript 泛型" }] },
  { streamMode: "messages" },
);
```

- `messages`：适合聊天界面，逐 Token/消息处理。
- `values`：读取完整状态。
- `updates`：只读取状态增量。

注意：不同 `streamMode` 的返回结构不同，循环处理前要先确认对应格式。

## 7. 结构化输出：优先让程序拿到可靠数据

不要只靠提示词要求模型“返回 JSON”，更推荐使用 `withStructuredOutput()` 绑定 Zod Schema。

```ts
import { z } from "zod";

const ArticleSchema = z.object({
  title: z.string(),
  tags: z.array(z.string()),
  difficulty: z.enum(["入门", "中级", "高级"]),
});

const structuredModel = model.withStructuredOutput(ArticleSchema, {
  strict: true,
});

const result = await structuredModel.invoke([
  { role: "user", content: "分析这篇文章并提取信息。" },
]);
```

| 方式 | 可靠性 | 问题 |
| --- | --- | --- |
| 提示词要求 JSON | 低 | 可能夹带解释、Markdown 代码块或格式错误 |
| `json_object` 模式 | 中 | 只保证是 JSON，不能保证字段和类型符合预期 |
| `withStructuredOutput()` + Zod | 高 | 字段和类型更稳定，适合程序消费 |

生产环境中，如无特殊原因，优先开启 `strict: true`。它能减少字段遗漏和格式错误，但可能略微增加延迟。

## 8. 本章最该记住的 5 件事

1. 模型抽象层降低供应商锁定，但不能忽略各 Provider 的能力和参数差异。
2. 快速验证用字符串模型名；长期项目和兼容 API 用显式实例化。
3. 对话上下文就是按顺序传入的消息列表。
4. 面向用户的长回复优先使用 `stream()`，提升等待体验。
5. 需要由代码稳定读取的数据，优先使用 `withStructuredOutput()` 与 Zod Schema。

## 9. 推荐练习

1. 使用同一个问题分别调用小模型和强模型，记录耗时、成本和回答质量。
2. 用 Zod Schema 从一段电影评论提取评分、情感倾向和演员名单。
3. 用 Express 的 SSE 将 `agent.stream()` 的内容实时推送到网页。
4. 安装 Ollama，对比本地模型和云端模型在简单代码生成任务上的表现。
